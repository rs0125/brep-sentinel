# Extra dependencies for the API layer only.
# brep_sentinel/ itself stays stdlib-only -- install these on top of the
# repo's existing requirements.txt.
fastapi>=0.111
uvicorn[standard]>=0.30
python-multipart>=0.0.9
